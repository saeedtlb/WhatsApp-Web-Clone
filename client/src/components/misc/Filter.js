const Filter = () => (
  <div className="filter">
    <label>
      <input type="checkbox" onClick={() => alert("Not implement yet")} />
      <span></span>
    </label>
    <h4>unread only</h4>
  </div>
);

export default Filter;
